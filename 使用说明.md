# 麻将趣味工具集 · PWA 版部署说明

本目录已改造为 **PWA（可安装、可离线）** 的完整文件组，配套 GitHub Pages 使用。

## 一、目录结构

```
.
├── index.html              主页（左右/顶部切换分页，已注册 Service Worker）
├── manifest.webmanifest    PWA 清单
├── sw.js                   Service Worker（离线缓存）
├── icon-192.png            ← 你上传（192×192）
├── icon-512.png            ← 你上传（512×512）
├── music1.mp3              ← 你上传（转动循环，根目录）
├── music2.mp3              ← 停止
├── music3.mp3              ← 完，没中
├── music4.mp3              ← 再来一把
├── music5.mp3              ← 再来两把
├── music6.mp3              ← 再来三把
├── module-skill.html       ① 技能抽取
├── module-wheel.html       ② 幸运转盘（音乐为根目录永久文件）
├── module-lottery.html     ③ 摇奖系统
├── module-jiasa.html       ④ 宝贝袈裟
├── module-horse.html       ⑤ 麻将赛马
├── module-tools.html       ⑥ 工具集合
└── season.html              ⑦ 赛季积分
```

> **所有文件都在根目录**，没有子文件夹。音乐文件 `music1.mp3 ~ music6.mp3` 与 `index.html` 平级存放。

## 二、你需要上传的 8 个文件

在 **GitHub 网页后台** 直接拖拽上传到**仓库根目录**即可：

| 目标位置 | 文件名 | 说明 |
|---|---|---|
| 仓库根目录 | `icon-192.png` | 192×192 应用图标 |
| 仓库根目录 | `icon-512.png` | 512×512 应用图标 |
| 仓库根目录 | `music1.mp3` | 转盘转动循环 |
| 仓库根目录 | `music2.mp3` | 转盘停止 |
| 仓库根目录 | `music3.mp3` | 抽中「完，没中」 |
| 仓库根目录 | `music4.mp3` | 抽中「再来一把」 |
| 仓库根目录 | `music5.mp3` | 抽中「再来两把」 |
| 仓库根目录 | `music6.mp3` | 抽中「再来三把」 |

> ⚠️ 文件名必须完全一致（wheel 页面按此固定读取，无需改代码）。
> 想换曲？直接在后台替换同名文件即可，**记得改缓存版本号**（见第五节）。

## 三、栏目顺序

导航已按你要求排列（左侧 / 手机顶部横滑条同步）：

| 序号 | 栏目 | 对应文件 |
|---|---|---|
| ① | 技能抽取 | module-skill.html |
| ② | 赛季积分 | season.html |
| ③ | 工具集合 | module-tools.html |
| ④ | 幸运转盘 | module-wheel.html |
| ⑤ | 摇奖系统 | module-lottery.html |
| ⑥ | 宝贝袈裟 | module-jiasa.html |
| ⑦ | 麻将赛马 | module-horse.html |

## 四、开启 GitHub Pages

1. 仓库 → **Settings → Pages**
2. Source 选 **Deploy from a branch** → 分支 `main`，目录选 `/ (root)`
3. 保存后等待约 30 秒，访问 `https://<用户名>.github.io/<仓库名>/`
4. Chrome 打开后地址栏会出现 **「安装应用」** 图标，即可添加到桌面/主屏

## 五、本地调试（VS Code）

```bash
# 在仓库目录下启动本地服务（Service Worker 必须 http(s) 才生效）
python3 -m http.server 8000
# 浏览器打开 http://localhost:8000
```

> 双击 `index.html`（`file://`）时 SW 不生效，但功能可用——**正式以 GitHub Pages 为准**。
> 音乐文件上传前，转盘页面会显示红色「缺失」标记；上传后即变绿。

## 六、改动了什么（相对原版）

1. **wheel 音乐**：移除 6 个 file 上传框，改为从 `music1.mp3 ~ music6.mp3` 永久加载，**文件放在仓库根目录**（无子文件夹），启动时自动检测文件是否存在并标注状态。
2. **栏目重排**：顺序改为 技能抽取 → 赛季积分 → 工具集合 → 幸运转盘 → 摇奖系统 → 宝贝袈裟 → 麻将赛马（`index.html` 导航与 iframe 已同步）。
3. **新增 PWA 三件套**：`manifest.webmanifest`、`sw.js`（缓存优先 + 离线回退首页）、`indexheiro.html` 注入 meta/link + SW 注册。
4. **路径修正**：`season.html` 从 `modules/` 子目录提到根目录。
5. **小修复**：`module-skill.html` 的 `lang="zh-CC"` 笔误改为 `zh-CN`。

### 更新缓存的两种方法（换音乐 / 换图标后必做）

- **推荐**：修改 `sw.js` 顶部的 `CACHE_VERSION`（如 `mj-tools-v2` → `v3`），浏览器会自动拉新；
- 或浏览器开发者工具 → Application → Storage → **Clear site data**，再刷新。

数据仍保存在各模块自己的 `localStorage` 中，模块间完全隔离，原功能 100% 保留。
